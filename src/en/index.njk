---
layout: base.njk
title: Home
description: Strategic Consulting - Critical Transition Phases
pageKey: "home"
---

<div class="bg-white">
    <!-- Hero Section Container - Lighter Teal Base for better visibility -->
    <div class="relative isolate overflow-hidden bg-teal-900 pb-16 pt-14 sm:pb-20">
      
      <!-- Animated Gradient Blobs - Lighter colors for contrast -->
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
          <div class="absolute -top-[10%] -left-[10%] w-96 h-96 bg-teal-500 rounded-full mix-blend-overlay filter blur-3xl opacity-40 animate-blob"></div>
          <div class="absolute top-[10%] right-[10%] w-96 h-96 bg-emerald-500 rounded-full mix-blend-overlay filter blur-3xl opacity-40 animate-blob animation-delay-2000"></div>
          <div class="absolute -bottom-[20%] left-[20%] w-96 h-96 bg-teal-400 rounded-full mix-blend-overlay filter blur-3xl opacity-40 animate-blob animation-delay-4000"></div>
          <div class="absolute bottom-[10%] right-[30%] w-72 h-72 bg-emerald-400 rounded-full mix-blend-overlay filter blur-3xl opacity-40 animate-blob animation-delay-2000"></div>
      </div>

      <!-- Backdrop Blur causing "Frosted Glass" effect over blobs -->
      <div class="absolute inset-0 bg-transparent backdrop-blur-3xl -z-10"></div>
      
      <!-- Particles.js container -->
      <div id="particles-js" class="absolute inset-0 -z-1 opacity-60 mix-blend-screen"></div>

    <div class="relative mx-auto max-w-7xl px-6 lg:px-8 z-10">

    <div class="mx-auto max-w-4xl py-16 sm:py-24">
      <!-- Announcement Banner (Hidden for now as no content provided)
      <div class="hidden sm:mb-8 sm:flex sm:justify-center">
        <div class="relative rounded-full px-3 py-1 text-sm/6 text-gray-600 ring-1 ring-gray-900/10 hover:ring-gray-900/20">
          Announcing our next round of funding. <a href="#" class="font-semibold text-emerald-600 hover:text-emerald-400"><span aria-hidden="true" class="absolute inset-0"></span>Read more <span aria-hidden="true">&rarr;</span></a>
        </div>
      </div>
      -->
      <div class="text-center">
        <h1 class="text-5xl font-semibold tracking-tight text-balance text-white sm:text-6xl">
            {{ i18n.hero.title[locale] }}
        </h1>
        <p class="mt-8 text-lg font-medium text-pretty text-teal-100 sm:text-xl/8">
            {{ i18n.hero.description[locale] }}
        </p>
        <div class="mt-10 flex items-center justify-center gap-x-6">
          <a href="/{{ locale }}/contact/" class="rounded-md bg-white px-3.5 py-2.5 text-sm font-semibold text-teal-900 shadow-xs hover:text-emerald-600 transition-colors duration-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
              {{ i18n.hero.cta[locale] }}
          </a>
          <a href="#services" class="text-sm/6 font-semibold text-white hover:text-emerald-400 transition-colors duration-200">{{ i18n.hero.learn_more[locale] }} <span aria-hidden="true">→</span></a>
        </div>
      </div>
    </div>

    </div>
  </div>
  
  <script src="/js/vendor/particles.min.js" defer></script>
  <script src="/js/hero-particles.js" defer></script>

  <!-- Services Section -->
  <div id="services" class="py-16 sm:py-24">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl lg:mx-0">
        <h2 class="text-4xl font-semibold tracking-tight text-pretty text-teal-900 sm:text-5xl">{{ i18n.services_new.title[locale] }}</h2>
        <p class="mt-6 text-lg/8 text-gray-700">{{ i18n.services_new.description[locale] }}</p>
      </div>
      <dl class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 text-base/7 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-2">
        {% for item in i18n.services_new.items %}
        <div class="flex flex-col border border-teal-800 rounded-2xl p-8 bg-teal-950">
          <dt class="text-2xl font-semibold tracking-tight text-white">{{ item.title[locale] }}</dt>
          <dd class="mt-4">
            <p class="font-medium text-teal-400">{{ item.tagline[locale] }}</p>
            <p class="mt-2 text-gray-300">{{ item.description[locale] }}</p>
          </dd>
        </div>
        {% endfor %}
      </dl>
  </div>

  <!-- Fundamentals Section -->
  {% include "components/fundamentals.njk" %}

  <!-- Sectors Section -->
  <div class="py-16 sm:py-24">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-7xl lg:mx-0 mb-16 border-t pt-8 border-gray-200">
        <h2 class="text-4xl font-semibold tracking-tight text-teal-900 sm:text-5xl">{{ i18n.sectors_home.title[locale] | safe }}</h2>
      </div>
      <dl class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 text-base/7 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-4">
        {% for item in i18n.sectors_home.items %}
        <div class="flex flex-col h-full">
           <dt class="text-3xl font-bold tracking-tight text-teal-900 mb-4">{{ item.title[locale] }}</dt>
           <dd class="flex flex-col flex-auto mb-6 {{ 'lg:order-first' if loop.index % 2 == 0 }}">
              <p class="font-semibold text-gray-900 mb-2">{{ item.tagline[locale] }}</p>
              <p class="text-gray-600">{{ item.description[locale] }}</p>
           </dd>
           <img src="{{ item.image }}" alt="{{ item.title[locale] }}" class="aspect-[4/5] w-full rounded-2xl object-cover bg-gray-100 grayscale {{ 'lg:order-first lg:mb-6' if loop.index % 2 != 0 else 'lg:order-last' }}">
        </div>
        {% endfor %}
      </dl>
    </div>
  </div>


</div>
