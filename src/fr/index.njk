---
layout: base.njk
title: Accueil
description: Conseil en Stratégie - Phases de Transition Critique
pageKey: "home"

---

<div class="bg-white">


  <div class="relative isolate px-6 pt-14 lg:px-8">

    <div class="mx-auto max-w-4xl py-16 sm:py-24">
      <div class="text-center">
        <h1 class="text-5xl font-semibold tracking-tight text-teal-900 sm:text-6xl">
            {{ i18n.hero.title[locale] | safe }}
        </h1>
        <p class="mx-auto mt-8 max-w-2xl text-lg font-medium text-pretty text-gray-500 sm:text-xl/8">
            {{ i18n.hero.description[locale] }}
        </p>
        <div class="mt-10 flex items-center justify-center gap-x-6">
          <a href="/{{ locale }}/contact/" class="rounded-md bg-teal-700 px-3.5 py-2.5 text-sm font-semibold text-white shadow-xs hover:bg-teal-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-700">
              {{ i18n.hero.cta[locale] }}
          </a>
          <a href="#services" class="text-sm/6 font-semibold text-teal-700 hover:text-teal-900 transition-colors duration-200">{{ i18n.hero.learn_more[locale] }} <span aria-hidden="true">→</span></a>
        </div>
      </div>
    </div>

  </div>

  <!-- Services Section -->
  <div id="services" class="bg-white py-16 sm:py-24">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl lg:mx-0">
        <h2 class="text-4xl font-semibold tracking-tight text-pretty text-teal-900 sm:text-5xl">{{ i18n.services_new.title[locale] }}</h2>
        <p class="mt-6 text-lg/8 text-gray-700">{{ i18n.services_new.description[locale] }}</p>
      </div>
      <dl class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 text-base/7 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-2">
        {% for item in i18n.services_new.items %}
        <div class="flex flex-col border border-teal-800 rounded-2xl p-8 bg-teal-950">
          <dt class="text-2xl font-semibold tracking-tight text-white">{{ item.title[locale] }}</dt>
          <dd class="mt-4">
            <p class="font-medium text-teal-400">{{ item.tagline[locale] }}</p>
            <p class="mt-2 text-gray-300">{{ item.description[locale] }}</p>
          </dd>
        </div>
        {% endfor %}
      </dl>
  </div>

  <!-- Fundamentals Section -->
  {% include "components/fundamentals.njk" %}

  <!-- Sectors Section -->
  <div class="bg-white py-16 sm:py-24">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-7xl lg:mx-0 mb-16 border-t pt-8 border-gray-200">
        <h2 class="text-4xl font-semibold tracking-tight text-teal-900 sm:text-5xl">{{ i18n.sectors_home.title[locale] | safe }}</h2>
      </div>
      <dl class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 text-base/7 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-4">
        {% for item in i18n.sectors_home.items %}
        <div class="flex flex-col h-full">
           <dt class="text-3xl font-bold tracking-tight text-teal-900 mb-4">{{ item.title[locale] }}</dt>
           <dd class="flex flex-col flex-auto mb-6 {{ 'lg:order-first' if loop.index % 2 == 0 }}">
              <p class="font-semibold text-gray-900 mb-2">{{ item.tagline[locale] }}</p>
              <p class="text-gray-600">{{ item.description[locale] }}</p>
           </dd>
           <img src="{{ item.image }}" alt="{{ item.title[locale] }}" class="aspect-[4/5] w-full rounded-2xl object-cover bg-gray-100 grayscale {{ 'lg:order-first lg:mb-6' if loop.index % 2 != 0 else 'lg:order-last' }}">
        </div>
        {% endfor %}
      </dl>
    </div>
  </div>


</div>
